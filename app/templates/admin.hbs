<div class="container">
  <h2 id="admin">WELCOME ADMIN</h2>
  <ul class="list-unstyled" id="main">
    {{#each model as |story|}}
      <h2 class="well" id="headline">{{story.headline}}</h2>
        <ul class="list-unstyled">
          <p>{{#link-to 'admin-story' story.id}}<img src={{story.photo}}>{{/link-to}}</p>
          <!-- {{update-story story=story update="update"}} -->
          <!-- {{delete-story story=model destroyStory="destroyStory"}} -->
        </ul>
    {{/each}}
  </ul>
  <!-- {{update-story story=story update="update"}} -->
  <h2>{{#link-to 'index'}}Home{{/link-to}}</h2>
</div>

{{new-story saveStory="saveStory"}}
