
<ul class="list-unstyled">
  {{#each model as |story|}}
    <h2 id="headline">{{#link-to 'story' story.id}}{{story.headline}}{{/link-to}}</h2>
      <ul class="list-unstyled">
        <p>{{#link-to 'story' story.id}}<img src={{story.photo}}>{{/link-to}}</p>
      </ul>
  {{/each}}
</ul>

<h2 class="well">{{#link-to 'admin'}}Admin{{/link-to}}</h2>
